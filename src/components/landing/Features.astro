---
---

<section class="py-24 md:py-28 px-4 md:px-8 max-w-6xl mx-auto" id="features">
  <div class="mb-16 reveal">
    <div class="section-label mb-4">Capabilities</div>
    <h2 class="text-3xl md:text-4xl font-bold tracking-tight leading-tight max-w-lg">
      Your computer, on autopilot.
    </h2>
  </div>

  <!-- Top row: 2 featured cards, asymmetric -->
  <div class="grid md:grid-cols-5 gap-5 mb-5">
    <!-- Work with any file -->
    <div class="feature-card-v2 md:col-span-3 p-8 md:p-10 reveal">
      <div class="flex items-start justify-between mb-6">
        <div class="w-12 h-12 bg-primary/10 rounded-xl flex items-center justify-center">
          <svg class="w-6 h-6 text-primary" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
            <path d="M22 19a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h5l2 3h9a2 2 0 0 1 2 2z"/>
          </svg>
        </div>
        <span class="text-xs font-mono text-muted-foreground/50 tracking-wider">01</span>
      </div>
      <h3 class="text-xl md:text-2xl font-bold mb-3">Work with any file</h3>
      <p class="text-muted-foreground leading-relaxed max-w-sm">
        Point to a folder and go. PDFs, spreadsheets, images, code — Caipi reads, understands, and works with whatever's on your machine. No uploading, no size limits.
      </p>
    </div>

    <!-- Run things locally -->
    <div class="feature-card-v2 md:col-span-2 p-8 md:p-10 reveal reveal-delay-1">
      <div class="flex items-start justify-between mb-6">
        <div class="w-12 h-12 bg-primary/10 rounded-xl flex items-center justify-center">
          <svg class="w-6 h-6 text-primary" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
            <path d="M13 2L3 14h9l-1 8 10-12h-9l1-8z"/>
          </svg>
        </div>
        <span class="text-xs font-mono text-muted-foreground/50 tracking-wider">02</span>
      </div>
      <h3 class="text-xl md:text-2xl font-bold mb-3">Run things locally</h3>
      <p class="text-muted-foreground leading-relaxed">
        Convert a video, resize 200 images, generate a chart from a CSV. AI writes the script, runs it, and hands you the result.
      </p>
    </div>
  </div>

  <!-- Middle row: 2 cards, reversed asymmetry -->
  <div class="grid md:grid-cols-5 gap-5 mb-5">
    <!-- Automate your workflow -->
    <div class="feature-card-v2 md:col-span-2 p-8 md:p-10 reveal reveal-delay-2">
      <div class="flex items-start justify-between mb-6">
        <div class="w-12 h-12 bg-primary/10 rounded-xl flex items-center justify-center">
          <svg class="w-6 h-6 text-primary" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
            <path d="M12 20h9M16.5 3.5a2.12 2.12 0 0 1 3 3L7 19l-4 1 1-4L16.5 3.5z"/>
          </svg>
        </div>
        <span class="text-xs font-mono text-muted-foreground/50 tracking-wider">03</span>
      </div>
      <h3 class="text-xl md:text-2xl font-bold mb-3">Automate your workflow</h3>
      <p class="text-muted-foreground leading-relaxed">
        Rename files by content, sort folders by date, clean up duplicates. Tasks that take hours become a single prompt.
      </p>
    </div>

    <!-- Research and save -->
    <div class="feature-card-v2 md:col-span-3 p-8 md:p-10 reveal reveal-delay-3">
      <div class="flex items-start justify-between mb-6">
        <div class="w-12 h-12 bg-primary/10 rounded-xl flex items-center justify-center">
          <svg class="w-6 h-6 text-primary" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
            <circle cx="11" cy="11" r="8"/>
            <path d="M21 21l-4.35-4.35"/>
          </svg>
        </div>
        <span class="text-xs font-mono text-muted-foreground/50 tracking-wider">04</span>
      </div>
      <h3 class="text-xl md:text-2xl font-bold mb-3">Research and persist</h3>
      <p class="text-muted-foreground leading-relaxed max-w-sm">
        Pull data from the web, summarize it, and save it straight to your notes. No copy-pasting between tabs — results go directly into your files.
      </p>
    </div>
  </div>

  <!-- Bottom row: 2 equal cards -->
  <div class="grid md:grid-cols-2 gap-5">
    <!-- See what's happening -->
    <div class="feature-card-v2 p-8 md:p-10 reveal reveal-delay-4">
      <div class="flex items-start justify-between mb-6">
        <div class="w-12 h-12 bg-primary/10 rounded-xl flex items-center justify-center">
          <svg class="w-6 h-6 text-primary" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
            <rect x="3" y="3" width="18" height="18" rx="2" ry="2"/>
            <line x1="3" y1="9" x2="21" y2="9"/>
            <line x1="9" y1="21" x2="9" y2="9"/>
          </svg>
        </div>
        <span class="text-xs font-mono text-muted-foreground/50 tracking-wider">05</span>
      </div>
      <h3 class="text-xl md:text-2xl font-bold mb-3">See what's happening</h3>
      <p class="text-muted-foreground leading-relaxed">
        Watch file changes, diffs, and generated outputs as they happen. A visual interface that shows you what the agent is doing — not just walls of text.
      </p>
    </div>

    <!-- Pick up where you left off -->
    <div class="feature-card-v2 p-8 md:p-10 reveal reveal-delay-5">
      <div class="flex items-start justify-between mb-6">
        <div class="w-12 h-12 bg-primary/10 rounded-xl flex items-center justify-center">
          <svg class="w-6 h-6 text-primary" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
            <circle cx="12" cy="12" r="10"/>
            <path d="M12 6v6l4 2"/>
          </svg>
        </div>
        <span class="text-xs font-mono text-muted-foreground/50 tracking-wider">06</span>
      </div>
      <h3 class="text-xl md:text-2xl font-bold mb-3">Pick up where you left off</h3>
      <p class="text-muted-foreground leading-relaxed">
        Sessions persist locally. Switch between projects, resume past conversations, keep your full context intact across days and weeks.
      </p>
    </div>
  </div>
</section>

<style>
  .feature-card-v2 {
    background: linear-gradient(160deg, rgba(18, 46, 56, 0.4) 0%, rgba(10, 26, 31, 0.6) 100%);
    border: 1px solid var(--border);
    border-radius: 16px;
    transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .feature-card-v2:hover {
    border-color: rgba(169, 216, 13, 0.25);
    transform: translateY(-3px);
    box-shadow: 0 16px 48px rgba(0, 0, 0, 0.2), 0 0 0 1px rgba(169, 216, 13, 0.05);
  }
</style>
